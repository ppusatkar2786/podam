test:
   override:
    - mkdir -p $CIRCLE_TEST_REPORTS/tmp/circle-junit.7SikF8i
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/tmp/circle-junit.7SikF8i \;
